import { StyleSheet, View, Text, Image } from "react-native";
import Info from "./Info";
const Result = (props) => {
  return (
    <View>
      <Info></Info>
      <Image></Image>
    </View>
  );
};

export default Result;
